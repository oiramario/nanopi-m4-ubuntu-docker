/dts-v1/;
 
/ {
    description = "boot script uImage";
    images {
         default = "script";
         script {
             description = "boot script";
             data = /incbin/("./boot.script");
             type = "script";
             compression = "none";
        };
    };
};
